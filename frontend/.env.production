# Production build: call the same-origin Vercel serverless API (see frontend/api + vercel rewrites).
# Local dev still uses frontend/.env with VITE_API_URL=http://localhost:8000/api (gitignored).
VITE_API_URL=/api
